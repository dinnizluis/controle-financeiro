---
agent: speckit.specify
---

When the input references a GitHub issue, `/speckit.specify` must update that issue with
specification traceability links after generating `spec.md` and the requirements checklist.
