---
description: "Refine user scope into an INVEST feature Issue with Gherkin acceptance criteria and UI regression coverage."
argument-hint: "Describe the user outcome and scope"
agent: speckit.refine
---

Refine the supplied scope into one backlog item. Create a GitHub Feature Issue only after I
explicitly approve the complete proposed body. Follow the repository backlog-refinement workflow.