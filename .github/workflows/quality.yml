name: quality

on:
  pull_request:
    branches:
      - development
      - uat
      - main
  push:
    branches:
      - development
      - uat
      - main

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: python -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/python -m pip install -e '.[dev]'
      - name: Test
        run: .venv/bin/python -m pytest -q --cov=controle_financeiro --cov-report=term-missing
      - name: Check formatting
        run: .venv/bin/python -m ruff format --check .
      - name: Lint
        run: .venv/bin/python -m ruff check .
      - name: Check types
        run: .venv/bin/python -m pyright
      - name: Export dependencies for audit
        run: .venv/bin/python -m pip freeze --exclude-editable > requirements-audit.txt
      - name: Audit dependencies
        run: .venv/bin/python -m pip_audit -r requirements-audit.txt